import { CsshighlighterDirective } from './csshighlighter.directive';

describe('CsshighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CsshighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
