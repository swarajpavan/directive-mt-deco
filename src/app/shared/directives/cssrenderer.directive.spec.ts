import { CssrendererDirective } from './cssrenderer.directive';

describe('CssrendererDirective', () => {
  it('should create an instance', () => {
    const directive = new CssrendererDirective();
    expect(directive).toBeTruthy();
  });
});
