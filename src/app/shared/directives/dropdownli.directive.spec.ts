import { DropdownliDirective } from './dropdownli.directive';

describe('DropdownliDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownliDirective();
    expect(directive).toBeTruthy();
  });
});
