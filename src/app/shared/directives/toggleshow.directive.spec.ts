import { ToggleshowDirective } from './toggleshow.directive';

describe('ToggleshowDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleshowDirective();
    expect(directive).toBeTruthy();
  });
});
